import {putForm} from "axios";

const UsersForm = () => {

    const [register, handleSubmit]= useF()


    return (
        <form onSubmit={}>
            <input type="text"/>
        </form>
    );
};

export {UsersForm};