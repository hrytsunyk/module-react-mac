const Launch = (launch) => {

    return (
        <div>
            {/*<h4>Mission name: {mission_name}</h4>*/}
            {/*<h4>Launch year: {launch_year}</h4>*/}
            {/*<h4>Mission patch: {mission_patch_small}</h4>*/}

        </div>
    );
};

export {Launch};